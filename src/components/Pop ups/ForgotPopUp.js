import { Modal, Text, View } from "react-native";
import React, { useState } from "react";
import styles from "./ForgotPopUpStyle";
import ReactNativeModal from "react-native-modal";
const ForgotPopUp = () => {
  
  return (
    
  );
};

export default React.memo(ForgotPopUp);
